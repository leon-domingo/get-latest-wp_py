# A Python 3 script to download the latest Wordpress (es_ES) version with no dependencies besides the standard library

Just clone the repo, cd into it and run the script

```shell
./get-lastest-wp.py
```

It requires **Python 3** and it's recommended the version **3.6** (or later).
